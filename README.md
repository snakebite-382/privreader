# This is a tool that reads privacy policies for you
It uses simple pattern matching to quickly identify important information in a privacy policy and provide sources to more information

## Running locally

Simply clone the git repo,
install nodejs 
run `npm install` in the git repo
and finally run either `npm run dev` for development mode or `npm run start` for deployment mode
